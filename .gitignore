supabase/.temp/

# Artefactos locais que ficaram no disco das branches React (estacionadas). Os
# lotes partem de main desde 2026-09-23, e sem estas linhas um `git add .`
# distraído metia centenas de MB no repositório — que é público.
node_modules/
dist/
*.local
.DS_Store

# Tooling local do Claude Code (skills instaladas; reproduzíveis do skills-lock.json)
.agents/
.claude/

# Artefactos de provas de ferramentas (Playwright MCP). Entradas específicas de
# propósito: o projeto tem PNG reais versionados (ícones), por isso nada de *.png.
.playwright-mcp/
/playwright-prova.png

# Biblioteca visual da Missão 26 — ficheiros de terceiros (peso, direitos de
# autor) e frames derivadas: ficam no disco, nunca no Git.
docs/design-references/mission-26/local/
docs/design references/
