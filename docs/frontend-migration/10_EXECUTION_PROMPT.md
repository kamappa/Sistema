# Prompt de execução para Claude Code

Usa este prompt quando a migração começar:

---

Vamos iniciar a migração da frontend do verdadeiro Sistema.

Antes de alterar código:

1. Lê `CLAUDE.md`.
2. Lê `SPEC-CLAUDE-CODE.md`.
3. Lê `SYSTEM-EVOLUTION-ROADMAP.md`.
4. Lê toda a pasta `docs/frontend-migration/` pela ordem indicada em
   `00_READ_ME_FIRST.md`.
5. Confirma a arquitetura atual no código real.
6. Confirma branch, git status e produção atual.
7. Confirma as ferramentas obrigatórias:
   - Claude in Chrome;
   - Context7;
   - Tavily;
   - Playwright;
   - Chrome DevTools;
   - frontend-design;
   - claude-code-setup;
   - skill-creator;
   - Superpowers;
   - ui-ux-pro-max;
   - 21st.dev Magic;
   - TypeScript LSP.
8. Não edites a `main`.
9. Não alteres produção.
10. Não reescrevas tudo de uma vez.

Objetivo técnico:

- nova frontend TypeScript + React + Vite;
- Supabase preservado;
- Vercel usado inicialmente para preview;
- Motion para UI;
- Three.js/R3F para universo e atmosfera;
- design system próprio;
- migração incremental;
- paridade de dados;
- forte melhoria visual.

Primeira resposta obrigatória:

- diagnóstico da arquitetura atual;
- confirmação de ferramentas;
- riscos;
- proposta da branch;
- plano da Fase 0 e Fase 1;
- estrutura de pastas;
- dependências propostas;
- diff esperado;
- critérios de aceitação;
- o que não será alterado.

Espera aprovação do Daniel antes de executar.

---
